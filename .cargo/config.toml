[build]
target = "kernel/x86_64-unknown-none.json" # Will save use some typing
target-dir = "target"

[unstable]
build-std-features = ["compiler-builtins-mem"]
build-std = ["core", "compiler_builtins"]
json-target-spec = true 

[term]
verbose = true