# Kernel

The bare-metal Rust kernel for the `rust-os` project.

> A minimal kernel written in Rust, designed for learning and experimentation with OS development.

## Overview

This crate contains the bare-metal kernel for the `rust-os` project.

The kernel is intentionally small and easy to read.

## License

Licensed under the terms in the repository root `LICENSE` file.
